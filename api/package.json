{
  "name": "basicapi",
  "version": "1.0.0",
  "description": "A Basic OAuth secured API deployed as an AWS lambda",
  "main": "handler.ts",
  "scripts": {
    "lint": "tslint -c tslint.json src/**/*.ts",
    "build": "tsc",
    "getCompanyList": "sls invoke local -f getCompanyList",
    "getCompanyTransactions": "sls invoke local -f getCompanyTransactions -p test/transactions.json"
  },
  "author": "Gary Archer",
  "license": "ISC",
  "dependencies": {
    "middy": "^0.19.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.14",
    "serverless": "^1.32.0",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1"
  }
}
