1. Revamp claims serialization since I must now always validate the JWT
   Redeploy and test

2. Put back correct cache code in baseCompositionRoot
   Put back performance threshold of 1000 in api.config.json

5. Tear down serverless2 items
   Then rename in branches
   Then deploy, test and merge
   Then update READMEs
   Then update blog instructions